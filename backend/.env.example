# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cloudclawmac

# JWT Secret (generate a secure random string for production)
JWT_SECRET=change-this-to-a-secure-random-string

# Orka API Configuration
ORKA_ENDPOINT=https://orka-api.macstadium.com
ORKA_USERNAME=your-email@example.com
ORKA_PASSWORD=your-password
# Alternatively, use a token:
# ORKA_TOKEN=your-orka-api-token

# Orka Defaults
ORKA_DEFAULT_VCPU=4
ORKA_DEFAULT_MEMORY=14
ORKA_BASE_IMAGE=ventura-base

# Trial Configuration
TRIAL_CREDITS=500
TRIAL_DURATION_DAYS=7
